# Use an official Python runtime as a parent image
FROM python:3.11-slim

# Set the working directory in the container
WORKDIR /app

# Copy the requirements file into the container at /app
# This is done first to leverage Docker's build cache
COPY requirements.txt .

# Install any needed packages specified in requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

# Copy the rest of your application's code into the container at /app
COPY . .

# The command to run your app using Gunicorn.
# This is the most critical line.
# It uses the $PORT variable provided by Render.
CMD ["gunicorn", "--workers", "1", "--bind", "0.0.0.0:$PORT", "--timeout", "120", "app:create_app()"]